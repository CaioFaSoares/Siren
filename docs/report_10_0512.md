# Siren - Status Report 10_0512

**Data:** 12 de Maio de 2026  
**Status Atual:** Migração v3 Completa / Ambiente Estabilizado / System Tray Funcional.

## 🚀 Migração Estrutural: Wails v2 ➡️ v3 (Alpha)

Concluímos a transição para o Wails v3 para resolver problemas críticos de estabilidade no Linux/Wayland e permitir uma arquitetura de daemon real.

### 1. Estabilidade e System Tray
- **Fim do SIGABRT**: A migração para o System Tray nativo do Wails v3 eliminou os conflitos de loop GTK que causavam o crash imediato da aplicação no Linux.
- **Menu da Bandeja**: Implementamos um menu funcional ("Mostrar Siren", "Desconectar Túnel", "Sair") que permite o controle da aplicação mesmo com a janela fechada.

### 2. Nova Arquitetura de Código
- **Procedural Application**: O `main.go` foi reescrito usando a nova API `application.New()`, permitindo o desacoplamento entre o ciclo de vida da aplicação e a interface gráfica.
- **Global Events & Services**: O `app.go` foi convertido para o padrão de *Service* do v3. A reatividade agora utiliza o sistema global de eventos (`application.Get().Event.Emit`), simplificando a comunicação entre o motor de áudio e a UI.

### 3. Orquestração de Ambiente (Nix + Task + Wails)
- **Taskfile Workflow**: Introduzimos o `Taskfile.yml` como o novo motor de automação. Ele gerencia o diretório de execução do frontend (`Nuxt 3`) e o build do backend de forma isolada e segura.
- **Sincronização de Dev Server**: O `build/config.yml` foi ajustado para forçar o Nuxt a rodar na porta `9245` e no host `127.0.0.1`, garantindo que o proxy do Wails encontre os assets sem erros de conexão ou conflitos de IPv6.
- **Nix Shell**: O `flake.nix` agora injeta automaticamente os binários locais do Go no PATH, permitindo o uso direto da CLI `wails` (v3).

## ⚠️ Observações de Desenvolvimento
- **Bindings**: As definições de Typescript agora são geradas em `frontend/bindings`. É recomendável rodar `wails generate bindings` sempre que houver mudanças na struct `App` ou em seus métodos.
- **Portas**: O ambiente está travado na porta `9245` para desenvolvimento para manter compatibilidade com o proxy interno do Wails v3 Alpha.

## 🛠️ Próximos Passos
1. Finalizar a integração visual no Nuxt usando as novas bindings geradas.
2. Testar o comportamento de "Hiding" (Ocultar) da janela ao clicar no "X" para garantir que o áudio continue fluindo sem interrupções.

---
*Relatório gerado automaticamente pelo assistente Antigravity.*
