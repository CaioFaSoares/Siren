# Siren - Relatório de Status #02 (10/05/2026)

## 1. Resumo Executivo
Nesta sessão, avançamos significativamente na base técnica do Siren. Concluímos a modelagem de domínio, a camada de persistência e as implementações reais dos motores de áudio para Linux e macOS. O projeto agora possui a inteligência necessária para rotear áudio e gerenciar dispositivos de hardware de forma estável.

## 2. Entregas Concluídas

### Épico 1: Core de Configuração e Estado
- **Modelagem de Domínio (`core/models.go`):** Estruturas robustas para `Device`, `AudioNode` e `TunnelConfig`.
- **Persistência (`core/store.go`):** Implementação do Viper para salvar configurações em `~/.config/siren/config.json`. Inclui CRUD seguro para concorrência (Mutex).

### Épico 2: Motores de Áudio (Engine)
- **Interface Universal (`core/engine.go`):** Definido o contrato `AudioEngine` e a factory `NewEngine()`.
- **Motor Linux (`core/engine_linux.go`):** 
    - Integração com PipeWire via `pw-cli`.
    - Captura dinâmica de ID de módulo via Regex.
    - Destruição limpa de módulos (fim dos zumbis).
    - Descoberta de hardware local.
- **Motor macOS (`core/engine_darwin.go`):**
    - Orquestração dos binários `roc-send` e `roc-recv`.
    - Gerenciamento de processos via `context.WithCancel`.
    - Descoberta de hardware CoreAudio.

## 3. Estado Atual do Código
- **Compilação:** O projeto compila com sucesso para Linux e macOS (via cross-compilation).
- **Dependências:** Viper (config) e Cobra (CLI) integrados.
- **Infra:** Nix Flake atualizado e funcional.

## 4. Próximos Passos (Próxima Sprint)
1. **Épico 3 (CLI/Daemon):**
    - Implementar comandos Cobra reais para ligar/desligar túneis via terminal.
    - Começar a lógica do Daemon que roda em background.
2. **Épico 4 (Wails Bridge):**
    - Vincular o `Store` e a `Engine` ao arquivo `app.go` para que a interface Nuxt possa listar dispositivos e iniciar conexões.

---
*Relatório gerado automaticamente pelo assistente Antigravity.*
